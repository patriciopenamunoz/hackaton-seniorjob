<nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
  <div class="container">
    <%= link_to 'SeniorJobs', root_path, class: 'navbar-brand js-scroll-trigger' %>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      Menú
      <i class="fas fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <% if empleador? %>
          <!-- Insertar lo que ve el empleador -->
          <%else%>

          <!-- Insertar lo que ve el abuelo -->
          <li class="nav-item">
            <%= link_to 'Ver empleos', pages_empleos_path, class: 'nav-link js-scroll-trigger' %>
          </li>
          <%end%>

          <!-- Insertar lo que ven los 2 -->
          <li class="nav-item">
            <%= link_to 'Cerrar sesión', destroy_user_session_path, method: 'delete', class: 'nav-link js-scroll-trigger' %>
          </li>
          
        <% else %>
          <!-- No lgueado -->
        <li class="nav-item">
          <%= link_to 'Iniciar sesión', new_user_session_path, class: 'nav-link js-scroll-trigger' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Registrarse', new_user_registration_path, class: 'nav-link js-scroll-trigger' %>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
