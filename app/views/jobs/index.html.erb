<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Jobs</h1>

  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Título</th>
        <th>Horario</th>
        <th>Sueldo</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @jobs.each do |job| %>
      <tr>
        <td><%= job.title %></td>
        <td><%= job.schedule %></td>
        <td><%= job.rent %></td>
        <td><%= link_to 'Editar', edit_job_path(job), class: 'btn btn-success' %></td>
        <td><%= link_to 'Eliminar', job, class: 'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Job', new_job_path %>
</div>
